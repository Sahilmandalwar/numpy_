import numpy as np

stats = np.array([[1,2,3],[4,5,6]]) 

# min
mini = np.min(stats,axis=1) # or np.min(stats)
print(mini)                 # or 1

# max 
maxi = np.max(stats,axis=0)
print(maxi)

# sum 
sum = np.sum(stats, axis = 0) # or np.sum(stats)
print(sum)                    # or 21
